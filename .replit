run = "python -m anti_filter_bridge.server --host 0.0.0.0 --port 8080"
modules = ["python-3.11"]

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "python -m anti_filter_bridge.server --host 0.0.0.0 --port $PORT"]
